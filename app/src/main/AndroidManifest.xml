<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Required for split tunneling: enumerate all installed packages -->
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission" />

    <!-- Custom permission guarding the tunnel control broadcast -->
    <permission
        android:name="net.mullvad.gotatunandroid.CONTROL_VPN"
        android:protectionLevel="normal" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.GotaTunAndroid">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.GotaTunAndroid"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".vpn.GotaTunService"
            android:permission="android.permission.BIND_VPN_SERVICE"
            android:exported="false"
            android:foregroundServiceType="specialUse"
            android:stopWithTask="false"
            tools:ignore="ForegroundServicePermission">
            <intent-filter>
                <action android:name="android.net.VpnService" />
            </intent-filter>
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="This service maintains an active WireGuard VPN tunnel." />
        </service>

        <!-- Quick Settings tile -->
        <service
            android:name=".vpn.TunnelTileService"
            android:exported="true"
            android:label="GotaTun VPN"
            android:icon="@drawable/ic_launcher_monochrome"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>

        <!-- Automation broadcast receiver (Tasker / Automate) -->
        <receiver
            android:name=".vpn.TunnelBroadcastReceiver"
            android:exported="true"
            android:permission="net.mullvad.gotatunandroid.CONTROL_VPN">
            <intent-filter>
                <action android:name="com.gotatun.android.action.SET_TUNNEL_UP" />
                <action android:name="com.gotatun.android.action.SET_TUNNEL_DOWN" />
            </intent-filter>
        </receiver>
    </application>

</manifest>